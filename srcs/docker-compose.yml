version: "3.5"

networks:
  mynetwork:
    name: mynetwork
    driver: bridge

services:  
  nginx:
    image: nginx
    build: ./nginx
    ports:
      - "443:443"      
    networks:
      - mynetwork
  
  mariadb:
    image: mariadb
    build: ./mariadb
    environment:
      - .env
    restart: always
    networks:
      - mynetwork

  wordpress:
    image: wordpress
    build: ./wordpress
    environment:
      - .env
    depends_on:
      - mariadb
    networks:
      - mynetwork
    
  
